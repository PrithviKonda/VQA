# deployment/Dockerfile.worker

# Placeholder worker Dockerfile for background tasks (if needed)
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/
COPY config.yaml .

# Example entrypoint (update as needed)
CMD ["python", "src/worker/main.py"]
